using System;

class Program
{
    static void Main()
    {
        int[] numeros = { 10, 20, 30, 40, 50 };

        for (int i = numeros.Length -1; i >= 0; i--)
        {
            Console.WriteLine(numeros[i]);
        }
    }
}

/* Hay algunos archivos c# en carpetas separadas, esto debido a que
perdi el comando que usaba para crear la carpeta obj y lo demas que
necesita jejejejejejejejeje. Si no encuentra el de c#, seguro esta en
una carpeta */